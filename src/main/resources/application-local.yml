logging:
  config: classpath:logback-spring.xml
spring:
  redis:
    port: 6379
    database: 0
    host: localhost
    username:
    password:
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    timeout: 5000ms
  application:
    name: dispatch-task
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        hbm2ddl:
          auto: none
    show-sql: true
  session:
    store-type: none
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wg_0520?useUnicode=true&characterEncoding=UTF8&zeroDateTimeBehavior=convertToNull
    username: root
    password: 123456
    platform: mysql
    type: com.alibaba.druid.pool.DruidDataSource

swagger:
  api:
    url: localhost:8029
