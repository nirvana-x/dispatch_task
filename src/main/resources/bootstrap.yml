server:
  port: 8029
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: dispatch-task
  profiles: local
  cloud:
    nacos:
      config:
        server-addr: localhost:8488
        context-path: nacos
        file-extension: yml

---
server:
  port: 8029
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: dispatch-task
  profiles: daily
  cloud:
    nacos:
      config:
        server-addr: localhost：8848
        context-path: nacos
        file-extension: yml

---
server:
  port: 8029
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: dispatch-task
  profiles: uat
  cloud:
    nacos:
      config:
        server-addr: localhost：8848
        context-path: nacos
        file-extension: yml

---
server:
  port: 8029
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: dispatch-task
  profiles: prod
  cloud:
    nacos:
      config:
        server-addr: localhost：8848
        context-path: nacos
        file-extension: yml
